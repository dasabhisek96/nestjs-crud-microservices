"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.DateValueConverter = void 0;
var DateValueConverter = (function () {
    function DateValueConverter() {
    }
    DateValueConverter.prototype.toView = function (date) {
        var t = typeof date;
        if (date) {
            if (t == 'number') {
                date = new Date(date);
                return this.f_d(date);
            }
            else if (t == 'string') {
                return date;
            }
            else if (t == 'object') {
                return this.f_d(date);
            }
        }
        else {
            return '';
        }
    };
    DateValueConverter.prototype.f_d = function (date) {
        var y = String(date.getFullYear());
        var m = date.getMonth() + 1;
        var hh = date.getHours();
        var mm = date.getMinutes();
        var s_mm = mm < 10 ? "0" + mm.toString() : mm.toString();
        var d = date.getDate();
        var s = y + '/' + m + '/' + d + " " + hh + ":" + s_mm;
        return s;
    };
    DateValueConverter.prototype.twoDigitPad = function (num) {
        return num < 10 ? "0" + num : num;
    };
    DateValueConverter.prototype.fromView = function (x) {
        var date = new Date();
        var y = Number(x.substr(0, 4));
        var m = Number(x.substr(5, 2)) - 1;
        console.log("fasdfjlakdsjfljasdlfj" + m);
        var d = Number(x.substr(8, 2));
        date.setFullYear(y);
        date.setMonth(m, d);
        console.log(date);
        return date;
    };
    return DateValueConverter;
}());
exports.DateValueConverter = DateValueConverter;
